# Cursor — Install Guide

```bash
git clone https://github.com/mathiaschu/ecommerce-bi.git
cd ecommerce-bi
pip3 install pandas numpy
bash scripts/install.sh cursor
```

Cursor will auto-load the rules when working with eCommerce CSV data.
